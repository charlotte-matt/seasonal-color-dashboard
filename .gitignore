# Raw H&M dataset — 3.3 GB, exceeds GitHub's 100 MB file limit.
# Download from Kaggle (H&M Personalized Fashion Recommendations) to rebuild data.js.
data/hm/

# Cached Google Trends responses (regenerated by fetch_trends.py)
data/trends_cache/

# Local Claude Code settings (user-specific, not for the repo)
.claude/settings.local.json

# Python / OS junk
__pycache__/
*.pyc
.DS_Store
.Rhistory
